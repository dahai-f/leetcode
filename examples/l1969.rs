//! [1969. 数组元素的最小非零乘积](https://leetcode-cn.com/problems/minimum-non-zero-product-of-the-array-elements/)
impl Solution {
    pub fn min_non_zero_product(p: i32) -> i32 {}
}

struct Solution;

fn main() {
    assert_eq!(Solution::rearrange_array(vec![]), vec![]);
    assert_eq!(
        Solution::num_of_strings(
            vec!["a", "b", "c"].iter().map(|&s| s.to_owned()).collect(),
            "aaaaabbbbb".to_owned(),
        ),
        2
    );
}
